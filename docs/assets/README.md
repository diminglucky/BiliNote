This directory stores README screenshots for VideoNote.

The `videonote-real-shot-*.png` files are copied from a real generated task in
`backend/static/screenshots` on 2026-06-20. They are intentionally separated
from the older `doc/image*.png` files so the README does not mix legacy demo
assets with current VideoNote output.
